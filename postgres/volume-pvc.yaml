apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: pg-disk-pvc-1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: linode-block-storage